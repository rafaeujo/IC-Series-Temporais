---
title: "Relatório de Progresso"
author: "Rafael Jordane de Souza Oliveira"
format: docx
editor: visual
---

## Breve introdução

Ao iniciarmos as produções da pesquisa, foi feita uma introdução ao estudo de series temporais e aos modelos de ajuste ARIMA. Para ter controle daquilo que está sendo testado, nesse caso os métodos de imputação de dados faltantes, foram gerados algumas variações de modelos de AR e ARMA, que foram posteriormente duplicados para analisar o Erro Médio causado por cada método de imputação em relação aos dados orginais gerados. Esses modelos foram testados com diferentes metodos de comparação e em diferentes ajustes. Os principais resultados foram sumarizados a seguir.

## Resultados

### Amostras de 100 observações:

Nessa subseção se encontram os resultados obtidos através do processo de imputação de dados faltantes à partir de diferentes métodos em modelos de séries temporais, de diferentes ajustes, todos com uma amostra 100 observações dessa forma

```{r}
milmedia <- read.csv("Planilhas/milmedia.csv")
milmedia
```

------------------------------------------------------------------------

**Tabela 1:** Resultados comparação dos erros em cada porcentagem de dados faltantes e modelo, com imputação da média e amostra de 1000 observações.

```{r}
cemmedia <- read.csv("Planilhas/cemmedia.csv")
cemmedia

```

------------------------------------------------------------------------

**Tabela 2:** Resultados comparação dos erros em cada porcentagem de dados faltantes e modelo, com imputação da média e amostra de 100 observações.

```{r}
milmediana <- read.csv("Planilhas/milmediana.csv")
milmediana
```

------------------------------------------------------------------------

**Tabela 3:** Resultados comparação dos erros em cada porcentagem de dados faltantes e modelo, com imputação da mediana e amostra de 1000 observações.

```{r}
cemmediana <- read.csv("Planilhas/cemmediana.csv")
cemmediana
```

**Tabela 4:** Resultados comparação dos erros em cada porcentagem de dados faltantes e modelo, com imputação da mediana e amostra de 100 observações.
